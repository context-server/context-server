name: CI

on:
  pull_request:
  merge_group:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  actions: write

env:
  CARGO_TERM_COLOR: always

jobs:
  msrv:
    name: MSRV
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88
      - name: Check minimum supported Rust
        run: cargo check --locked

  test:
    name: Test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            libssl-dev \
            pkg-config

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Test
        run: cargo test --locked

      - name: Release binary smoke test
        run: |
          cargo build --release --locked
          ./target/release/context-server --help
          if ldd ./target/release/context-server | grep -q libonnxruntime; then
            echo 'error: binary dynamically links libonnxruntime' >&2
            exit 1
          fi

  # Same platforms as release.yml. Native Ubuntu 24.04 builds (not Docker) so
  # Swatinem/rust-cache can reuse the release target between runs. Containerfile
  # remains for local podman/docker wheel builds.
  linux:
    name: Linux wheel (${{ matrix.arch }})
    needs: [test]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: ubuntu-24.04
            arch: x86_64
          - os: ubuntu-24.04-arm
            arch: aarch64
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            libssl-dev \
            patchelf \
            pkg-config \
            python3 \
            python3-pip

      - uses: dtolnay/rust-toolchain@stable

      # Restore before set-version so CalVer rewrites of Cargo.toml/lock don't
      # bust the key. Save even on failure so a long compile isn't wasted.
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: wheel-linux-${{ matrix.arch }}
          cache-on-failure: true

      - name: Resolve version
        id: ver
        run: |
          set -euo pipefail
          VERSION="$(./scripts/next-calver.sh)"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Using VERSION=$VERSION"
          chmod +x scripts/*.sh
          ./scripts/set-version.sh "$VERSION"

      # Call maturin on the host (not maturin-action): the action injects its own
      # --compatibility and conflicts with manylinux_2_39. glibc 2.39 on
      # ubuntu-24.04 matches the Containerfile / PyPI tag.
      - name: Install maturin
        run: sudo python3 -m pip install --break-system-packages 'maturin[patchelf]>=1.0,<2.0'

      - name: Build wheel
        env:
          OPENSSL_NO_VENDOR: '1'
        run: |
          set -euo pipefail
          mkdir -p dist
          maturin build --release --locked --compatibility manylinux_2_39 -o dist
          ls -la dist

      - name: Build sdist
        if: matrix.arch == 'x86_64'
        run: maturin sdist -o dist

      - name: Install and smoke-test wheel
        if: matrix.arch == 'x86_64'
        run: |
          python3 -m venv /tmp/context-server-smoke
          /tmp/context-server-smoke/bin/pip install dist/*.whl
          /tmp/context-server-smoke/bin/context-server --help
          if ldd /tmp/context-server-smoke/bin/context-server | grep -q libonnxruntime; then
            echo 'error: installed wheel dynamically links libonnxruntime' >&2
            exit 1
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.arch }}
          path: dist/*

  macos:
    name: macOS wheel (aarch64)
    needs: [test]
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: wheel-macos-aarch64
          cache-on-failure: true

      - name: Resolve version
        id: ver
        run: |
          set -euo pipefail
          VERSION="$(./scripts/next-calver.sh)"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          chmod +x scripts/*.sh
          ./scripts/set-version.sh "$VERSION"

      # ort does not ship prebuilt ONNX Runtime for x86_64-apple-darwin.
      - uses: PyO3/maturin-action@v1
        with:
          target: aarch64-apple-darwin
          args: --release --locked --out dist
          manylinux: 'off'

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-aarch64
          path: dist/*
